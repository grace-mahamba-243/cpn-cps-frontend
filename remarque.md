1. Respect de la structure existante : le projet est deja structure avec des dossiers comme application, composants, pages, modules, etc. Chaque nouvel element (fichier, composant, style) doit etre place dans le dossier adequat.

2. Gestion des images : si une page contient une photo, il faut la recuperer, l'enregistrer dans src/assets et l'utiliser correctement dans le code.

3. Reutilisation et creation de composants : avant de creer un nouveau composant, il faut d'abord verifier si un composant existant dans src/composants peut etre reutilise. Si ce n'est pas le cas, le composant doit etre cree proprement.

4. Commentaires en francais : tous les commentaires integres dans le code, ainsi que toutes les explications fournies, doivent etre rediges en francais.

5. Pour chaque page creee, il faut aussi creer les endpoints necessaires cote backend dans cpn-cps-nestjs, ainsi que la connexion a la base de donnees, en respectant la structure et la logique du projet.

6. A la fin de chaque fonctionnalite implementee, il faut fournir la liste des tables a integrer dans la base de donnees.